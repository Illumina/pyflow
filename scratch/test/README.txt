
pyflow test scripts:

This directory includes pyflow unit_tests and run tests which can be executed on both local and sge.

Both types of tests are kicked off by test_release_tarball.bash -- this is
what's actually kicked off by the CI system on each push.

